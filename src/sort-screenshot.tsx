import { ActionPanel, Action, Icon, List, closeMainWindow } from "@raycast/api";
import fs from "fs"; 

export default function Command() {
	// Need to add a config option 
	// wherever they want the screenshots to go thats the dir I should open
	<List>

	</List>
}
